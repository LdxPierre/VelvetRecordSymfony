{% extends 'base.html.twig' %}

{% block title %}Modifier
	{{disc.title}}
{% endblock %}

{% block body %}
	<a class='btn btn-primary mb-4' href="{{ path('app_disc_index') }}">Retour</a>
	<h1>Modifier
		{{disc.title}}</h1>
	{{ include('disc/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
	
	{{ include('disc/_delete_form.html.twig') }}

{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Modifier {{artist.name}}{% endblock %}

{% block body %}
<div class="d-flex align-items-center mb-4">
    <a class='btn btn-primary me-4' href="{{ path('app_artist_index') }}">Retour</a>
    <h1 class=''m-0>Modifier {{artist.name}}</h1>
</div>

{{ include('artist/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

<div class="col-6 mx-auto mt-2 d-flex">
    {{ include('artist/_delete_form.html.twig') }}
</div>
{% endblock %}

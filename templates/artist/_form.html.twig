{{ form_start(form, {'attr': {'novalidate':'novalidate'}})}}
<div class="mx-auto col-6 d-flex flex-column gap-2">
    {{form_row(form.name, {'label': 'Nom de l\'artiste'})}}
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
